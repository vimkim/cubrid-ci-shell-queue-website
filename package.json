{
  "name": "cubrid-ci-shell-queue",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local dashboard for the shared CUBRID CircleCI test_shell queue",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "queue": "node queue.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
